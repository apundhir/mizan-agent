"""The fifth of the five things an agent is: a committed eval set, scored deterministically.

`harness.py` holds the scorer, `cases/<agent>/*.json` the cases, and `test_agent_evals.py` the
assertions - including the one that matters most, that the scorer rejects a wrong answer.
"""
