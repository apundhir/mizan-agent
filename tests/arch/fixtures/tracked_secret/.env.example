# A committed example naming the variable and carrying no value. The guard must NOT flag this:
# one that did would be removed within a week, and then the real leak would go unnoticed.
ANTHROPIC_API_KEY=sk-ant-xxx
MIZAN_LLM_PROVIDER=replay
