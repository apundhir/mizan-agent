"""VIOLATION FIXTURE - the same violation wearing a hat.

`tda.helpers` is not on any forbidden list, so a grep for `anthropic` under metrics/ finds
nothing. The reach is what matters, not the spelling of the import.
"""

from tda.helpers import CLIENT


def guests_by_nationality(records: list[object]) -> dict[str, int]:
    return {}
