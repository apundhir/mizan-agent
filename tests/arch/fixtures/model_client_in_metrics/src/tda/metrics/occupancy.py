"""VIOLATION FIXTURE - the direct case.

At 6pm before a demo, a country label will not map and the fastest fix in the world is to ask
the model. A reviewer looking at a three-line diff will approve it. This fixture is that diff.
"""

import anthropic


def occupancy_pct(records: list[object]) -> float:
    client = anthropic.Anthropic()
    return 0.0
